# Datasource connection details are injected by Testcontainers via @ServiceConnection
# (see TestcontainersConfiguration) — do not hardcode spring.datasource here.
jwt:
  secret: "test-secret-that-is-exactly-32b!!"